guard 'rake', :task => 'jekyll' do
  watch(%r{_source/_layouts/_haml/_jekyll})
end

guard 'rake', :task => 'haml_pages' do
  watch(%r{_source/_layouts/_haml/_pages})
end

guard 'rake', :task => 'sass_style' do
  watch(%r{_source/_layouts/_sass/})
end

